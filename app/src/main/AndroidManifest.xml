<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ekarachinskaya.k_tehn">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Каскад технологии"
        android:roundIcon="@mipmap/logo2"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".nav"
            android:label="О нас"
            android:theme="@style/AppTheme.NoActionBar" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".podxod"
            android:label="@string/action_podhod"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".one_C"
            android:label="@string/title_activity_one__c"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MapsKarta"
            android:label="@string/title_activity_maps_karta"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Uslugi"
            android:label="@string/title_activity_prob"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".spravka"
            android:label="Техническая поддержка"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".obr_sv"
            android:label="@string/title_activity_obr_sv"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".proooooba"
            android:label="@string/title_activity_proooooba"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".contact"
            android:label="@string/title_activity_contact"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>